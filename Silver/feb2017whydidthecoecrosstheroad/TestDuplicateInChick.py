from bisect import *
print(bisect_left([1,2,3], 4))# leftmost place in the sorted list to insert the given element
print(bisect_right([1,2,3], 4)) # rightmost place in the sorted list to insert the given element